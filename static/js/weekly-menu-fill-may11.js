(function () {
  // Retired May 11 auto-fill script.
  // Weekly workflow now uses the printable menu as the review/approval step first.
  // Do not auto-create recipes or weekly menu entries until the printed menu is approved.
})();
